{
  "name": "vst-plugin",
  "scripts": {
    "clean": "rm -rf build/",
    "test": "ctest",
    "start": "node scripts/open.js",
    "generate": "node scripts/generate.js",
    "build": "node scripts/build.js",
    "clean-build": "npm run clean && npm run generate && npm run build"
  }
}
